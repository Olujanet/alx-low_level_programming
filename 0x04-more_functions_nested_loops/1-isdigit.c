#include "main.h"
/**
 * _isdigit - Checks for a digit from 0 to 9
 * @c: inter to be checked
 * Return: 1 if inter is a digit, 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
